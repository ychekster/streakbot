# Зависимости API-сервера TMA.
#
# Backend переиспользует код бота (модели, репозиторий, базовую инфраструктуру БД),
# поэтому ему нужны и зависимости бота — подключаем их одной строкой из корня.
-r ../../requirements.txt

# Сам API-сервер.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
