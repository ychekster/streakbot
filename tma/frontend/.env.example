# Базовый URL API-сервера TMA (tma/backend). Читается на этапе сборки Vite.
# Локальная разработка (бэкенд на TMA_PORT по умолчанию):
VITE_API_BASE_URL=http://localhost:8000
# В продакшене — публичный адрес API, например:
# VITE_API_BASE_URL=https://api.example.com
